{port_specs, [
    {"priv/apod.so", ["c_src/*.c"]}
]}.

{port_env, [
    % Development compilation
    {".*", "CFLAGS", "$CFLAGS -g -Wall -Werror -fPIC"},
    {".*", "LDFLAGS", "$LDFLAGS -lJudy"}

    % Production compilation
    % {".*", "CFLAGS", "$CFLAGS -Wall -Werror -DNDEBUG -O3"}
]}.
