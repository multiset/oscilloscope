{["org", org_name], oscilloscope_auth_http_org, []}.
{["org", org_name_or_id, "teams", team_name_or_id], oscilloscope_auth_http_team, []}.
{["user", email], oscilloscope_auth_http_user, []}.

{["metrics"], oscilloscope_auth_http, []}.
{["metrics", org_name], oscilloscope_auth_http, []}.
{["rules", org_name, team_name], oscilloscope_auth_http, []}.

PUT /org/<org name>
PUT /org/<org name or id>/teams/<team name>
PUT password=<password> /user/<email>
POST user=<email or id> /org/<org name or id>/join
POST user=<email or id> /org/<org name or id>/leave
POST user=<email or id> /team/<org name or id>/join
POST user=<email or id> /team/<org name or id>/leave

GET /metrics
POST <propkey>=<propvalue regex>&<pk>=<pvr>&etc /metric
GET /metrics/<org name>
POST <propkey>=<propvalue regex>&<pk>=<pvr>&etc /metric/<org name>
GET /rules/<org name>/<team name>
PUT <prop key>=<prop value regex>&<pk>=<pvr>&etc /rules/<org name>/<team name>
DELETE <prop key>=<prop value regex>&<pk>=<pvr>&etc /rules/<org name>/<team name>
