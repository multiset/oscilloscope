{application, osc_meta, [
    {description, "App for handling metric-related metadata"},
    {vsn, git},
    {registered, [osc_meta_sup]},
    {applications, [
        kernel,
        stdlib,
        osc_sql,
        osc_http
    ]},
    {mod, {osc_meta_app, []}},
    {env, [
        {default_window_configuration, [
            {rectangular, avg, 10, 1000},
            {rectangular, avg, 60, 1000},
            {rectangular, avg, 3600, 1000}
        ]}
    ]}
]}.
