%% -*- mode: erlang -*-
%% -*- tab-width: 4;erlang-indent-level: 4;indent-tabs-mode: nil -*-

{application, osc_kafka, [
    {description, ""},
    {vsn, git},
    {registered, [osc_kafka_sup]},
    {applications, [
        kernel,
        stdlib,
        kofta,
        poolboy,
        osc
    ]},
    {mod, {osc_kafka_app, []}},
    {env, [
        {partitions, []},
        {topic_name, <<"porter">>}
    ]}
]}.
